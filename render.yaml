services:
  - type: web
    name: flappybird-duatop
    env: python
    branch: branch-PY
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
